# Lean Apple-Silicon runtime for the TripoSplat MLX/MPS hybrid path.
# The MLX port package itself is installed separately with --no-deps so that
# FLUX/Klein, FastAPI, diffusers, transformers, etc. are not pulled in.
torch>=2.0.0
torchvision>=0.15.0
mlx>=0.15.0
numpy>=1.24.0
safetensors>=0.4.0
Pillow>=10.0.0
tqdm>=4.65.0
huggingface-hub[hf_xet]>=0.21.0
